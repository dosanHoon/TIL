# localstorage

```javascript
    JSON.parse(localStorage.getItem("name"))
    //Object 값 가져오기

    localStorage.setItem("name",JSON.stringify({key : value}))
    //Object 값 가져오기

    localStorage.clear()
    //localStorage 삭제
```

